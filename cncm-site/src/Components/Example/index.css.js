/**
 * This is an example CSS file. Don't edit the first two or last two lines, but
 * change the intervening CSS.
 */

export default function styles() {
  return {
    component: {
      textAlign: "center"
    }
  };
}
